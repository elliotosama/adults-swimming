Options -Indexes
Options -MultiViews

# Block access to sensitive files
<FilesMatch "(^\.ht|\.env|\.sql|\.log|\.json|composer\.lock)">
    Require all denied
</FilesMatch>

RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
